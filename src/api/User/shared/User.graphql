type User {
  # ! means required
  id: Int!
  email: String
  veifiedEmail: Boolean!
  firstName: String!
  lastName: String!
  fullName: String
  age: Int
  password: String
  phoneNumber: String
  veifiedPhoneNumber: Boolean!
  profilePhoto: String # url
  isDriving: Boolean!
  isRiding: Boolean!
  isTaken: Boolean!
  lastLng: Float
  lastLat: Float
  lastOrientation: Float
  createdAt: String!
  updatedAt: String
}

type Query {
  user: User
}
