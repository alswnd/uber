type Chat {
  id: Int!

  # relationship with Chat.graphql
  messages: [Message]! # has many messages (one to many)
  passenger: User!
  passengerId: Int!
  driver: User!
  driverId: Int!
  ride: Ride!
  createdAt: String!
  updatedAt: String
}
